require 'spec_helper'

describe BitcoinTicker do
  it 'has a version number' do
    expect(BitcoinTicker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
